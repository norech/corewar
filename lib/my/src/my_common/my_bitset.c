/*
** EPITECH PROJECT, 2020
** my_bitset
** File description:
** Source code
*/
#include <my.h>
#include <stdio.h>

char my_bitset(char byte, int position, bool enabled)
{
    return (BITSET(byte, position, enabled));
}